/**
 * This component is used to display a 404 page not found error.
 * It is used when the user tries to access a page that does not exist.
 * @returns
 */
const PageNotFound = () => {
  return <h1>404 - Sorry Bro, Seite nicht gefunden!</h1>;
};

export default PageNotFound;
